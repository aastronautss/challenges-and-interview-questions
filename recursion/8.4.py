# Write a function to compute a power set of a given set.

# def powerset(seq):
#     lst = list(seq)
#     result = [lst]
#     if len(lst) == 0:
#         return result
#     return result + powerset(lst)
